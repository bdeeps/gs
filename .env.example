DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gurbani
OPENAI_API_KEY=sk-your-openai-key

# Self-hosted E5 (recommended): same model as HF, no re-seed, no HF billing.
# Local: docker compose up -d embedding  →  http://localhost:8100
# Railway all-in-one Dockerfile: leave unset (sidecar starts on 127.0.0.1:8100).
# Railway separate embedding service: https://your-embedding.up.railway.app (include https://)
EMBEDDING_SERVICE_URL=http://localhost:8100
# START_EMBEDDING_SIDECAR=0

# Only if EMBEDDING_SERVICE_URL is unset (e.g. one-off seed without local service):
# HF_API_KEY=hf-your-huggingface-key

EMBEDDING_MODEL=intfloat/multilingual-e5-large
# Optional override (default is Hugging Face router hf-inference):
# HF_INFERENCE_BASE_URL=https://router.huggingface.co/hf-inference
PG_POOL_MAX=5

# Gurudwara auth (set SESSION_SECRET to a long random string in production)
SESSION_SECRET=change-me-to-a-long-random-string-at-least-16-chars
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Gurbani Voice Searcher

# Plinth (https://plinth.tools) — transactional email for signup / verify / reset
PLINTH_API_KEY=plinth_production_your_key_here

# Optional seed inputs:
# SHABADOS_SQLITE_PATH=./data/shabados.sqlite
# SHABADOS_JSON_PATH=./data/sggs-lines.json
# Defaults to the official stable ShabadOS SQLite release if omitted:
SHABADOS_DOWNLOAD_URL=https://github.com/shabados/database/releases/download/4.8.7/database.sqlite
# SEED_LIMIT=100
# SEED_SOURCE_ID=1
# SEED_OFFSET=0
# SEED_DELAY_MS=150
